{
  "name": "risk-compass",
  "version": "1.0.0",
  "description": "Risk management application",
  "main": "backend/dist/src/main.js",
  "scripts": {
    "build": "cd backend && npm install && npm run build",
    "start": "cd backend && node dist/src/main.js",
    "dev": "cd backend && npm run start:dev"
  },
  "engines": {
    "node": "18.x"
  }
}
